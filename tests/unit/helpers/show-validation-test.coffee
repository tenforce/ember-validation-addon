`import { showValidation } from 'dummy/helpers/show-validation'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | show validation'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = showValidation 42
  assert.ok result
