`import { eql } from 'dummy/helpers/eql'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | eql'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = eql 42
  assert.ok result
