`import { capitalizeParameterName } from 'dummy/helpers/capitalize-parameter-name'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | capitalize parameter name'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = capitalizeParameterName 42
  assert.ok result
