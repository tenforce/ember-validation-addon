<tr>
    <th>#</th>
    <th>id</th>
    <th>type</th>
    {{#each parameters as |param|}}
        {{#unless (eq param "uuid") }}
            {{#unless (eq param "type") }}
                <th>{{param}}</th>
            {{/unless}}
        {{/unless}}
    {{/each}}
</tr>
